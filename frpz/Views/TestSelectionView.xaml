<Window x:Class="frpz.Views.TestSelectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вибір тесту" Height="400" Width="600">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Оберіть тест:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />
            <ListBox Name="TestsList" Width="400" Height="200" DisplayMemberPath="Title" 
                     ItemsSource="{Binding AvailableTests}" SelectedItem="{Binding SelectedTest}" />
            <Button Content="Почати тест" Width="200" Margin="0,10,0,0" Click="StartTest_Click" />
        </StackPanel>
    </Grid>
</Window>